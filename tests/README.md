# Plotting test results (Linux)

Install gnuplot:
```shell
sudo apt-get install gnuplot
```

Enter gnuplot in this folder:
```shell
gnuplot
```

Enter this cmd in the interactive gnuplot shell:
loat "test.plt"
